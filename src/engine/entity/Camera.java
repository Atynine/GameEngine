package engine.entity;

public class Camera extends Entity {
    public Camera(float x, float y, float w, float h) {
        super(x, y, w, h);
    }

    @Override
    public void update(long delta) {
        //TODO update camera movement
    }
}
